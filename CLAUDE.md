# Article 6.2 Policy Wiki — Claude操作ガイド

このファイルはClaudeへの指示書です。このwikiをどう使うか・更新するかを記載しています。

## このWikiとは
パリ協定Article 6.2の各国政策状況をトラッキングするMarkdown Wikiです。
LLMが書き・維持します。手動編集は最小限にしてください。

## フォルダ構造
```
article6-wiki/
├── raw/          ← 生データ（ソース文書）。LLMが読む。手動追加もOK
├── wiki/         ← LLMが生成・更新するWiki本体（MkDocs Material、docs_dir）
│   ├── index.md            全体インデックス（常に最新に保つ）
│   ├── matrix.md           二国間合意マトリクス（最重要）
│   ├── itmo_tracker.md     確認済みITMO移転の一元管理ページ（移転実績の正はここ）
│   ├── _sources.md         自動収集ソース候補
│   ├── singapore_ia_system.md / projects_database.md  制度詳細
│   ├── countries/          国別ページ（調達国＋ホスト国）
│   ├── buyers/             CORSIA・IMO等の非国家調達主体
│   ├── agreements/         協定別ページ
│   └── concepts/           概念解説
├── outputs/      ← Q&A出力・クロールログ（gitignore対象、ローカルのみ）
├── scripts/      ← send_weekly_update.ps1（週次更新メール送信）
├── .github/workflows/deploy.yml  ← GitHub Pages自動デプロイ（push時にmkdocs build）
└── mkdocs.yml    ← サイト設定・nav（ページ追加時は必ずnavにも追加）
```

## よく使うコマンド（Claudeへの指示例）

### 情報更新
```
「[国名]の最新情報をウェブで調べて、rawとwikiを更新して」
「matrix.mdを最新の状態に更新して」
```

### Q&A（回答をoutputsに保存）
```
「東南アジアでArticle 6.2が最も進んでいる国はどこか、理由付きで教えて。outputs/に保存して」
「シンガポールの買国としての戦略を投資家向けに1ページにまとめて」
「インドネシアとシンガポールのIA交渉の障壁を整理して」
```

### ヘルスチェック
```
「wikiの情報で矛盾・古い情報がないかチェックして」
「更新が必要なページをリストアップして」
```

### 新しいrawデータを追加した後
```
「raw/に新しいファイルを追加した。内容を読んで対応するwikiページを更新して」
```

## バッチ更新プロセス（Claudeが必ず守ること）

**Q&Aセッション中はWikiファイルを直接編集しない。**

1. ユーザーからの質問への回答中に判明したWiki更新事項は、`outputs/pending_updates.md` に追記する
2. `outputs/pending_updates.md` の書式：
   ```
   ## [YYYY-MM-DD] セッション中判明事項
   - 対象ファイル: wiki/countries/xxx.md
   - 変更内容: [具体的な更新内容]
   - ソース: [URL]
   ```
3. Wikiへの実反映は、**クロール実行後**または**ユーザーが明示的に「Wikiに反映して」と指示したとき**のみ行う
4. クロール後の反映時は、クロール結果と pending_updates.md の内容を合わせて1回のコミットにまとめる
5. 例外：レイアウト修正・構造変更・新規ページ作成など、クロールと無関係の改善作業は即時実行してよい

**理由**: GitHubへのpush頻度を抑え、クロール結果との整合性を保つため。

## Wikiの品質ルール（Claudeが守るべきこと）
1. 情報の確信度が低い場合は `（要確認）` または `（未確認）` と明記
2. ソースは必ず `raw/` ファイルへのバックリンクで示す
3. `最終更新:` 日付を必ず更新する
4. ステータス記号は `matrix.md` の凡例を使う（— / 🔴 MOU / 🟡 IA署名 / 🟢 ITMO移転済み / ⚠️ 停止）
5. 新しい概念を導入する場合は `wiki/concepts/` にページを作る
6. **授権≠移転**：二国間協定・授権（authorization）・購入契約・RFP落札・クレジット発行決定は、いずれも「ITMO移転済み（🟢）」ではない。🟢は登録簿間の国際移転完了が一次ソースで確認できる場合のみ。確認済み移転の正典は `wiki/itmo_tracker.md` — 新たな移転を確認したら必ず同ページに追記し、matrix.md・国別ページと整合させる
7. ページを新設したら `mkdocs.yml` の nav に追加する

## 対象国（初期）
🇯🇵 日本 | 🇸🇬 シンガポール | 🇮🇩 インドネシア | 🇵🇭 フィリピン | 🇱🇦 ラオス

## 将来追加したい国（候補）
タイ、ベトナム、カンボジア、マレーシア、パプアニューギニア、ガーナ
